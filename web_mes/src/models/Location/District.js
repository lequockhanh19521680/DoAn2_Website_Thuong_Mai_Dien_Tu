export class DistrictModel{
    constructor({id, provinceID, name, prefix, createTime, updateTime}){
        this.id = id;
        this.provinceID = provinceID;
        this.name = name;
        this.prefix = prefix;
        this.createTime = createTime;
        this.updateTime = updateTime;
    }
}
